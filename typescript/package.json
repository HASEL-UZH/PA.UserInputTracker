{
  "name": "user-input-tracker",
  "version": "1.1.0",
  "description": "A cross-platform user input tracker",
  "author": {
    "name": "HASEL",
    "email": "dev@hasel.dev"
  },
  "engines": {
    "node": "^24.15.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "test": "jest --config jest.config.json",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^25.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "uiohook-napi": "^1.5.5"
  }
}
